<?php

/**
 * @package     {{ generatorName }}
 * @subpackage  Generator
 *
 * @copyright   Copyright (C) Yepr, Herman Peeren. All rights reserved.
 * @license     GNU General Public License version 3 or later; see LICENSE.txt
 */

namespace {{ namespace }};

use {{ baseNamespace }}\RuleDrivenGenerator;

/**
{% if summary %} * {{ summary }}
 *
{% endif %} * Generated by Gen-gen from the modelled generator "{{ generatorName }}".
 * Do not edit: what this class does is the {{ ruleCount }} rule{{ ruleCount == 1 ? '' : 's' }} whose ids begin with
 * `{{ prefix }}`, and those are edited in the model.
 *
 * There is no logic here on purpose. A rule file does not run by itself -
 * Joomla asks a component for a list of generators and runs them in order - so
 * something has to be a class, claim a slice of the rules and sit in that
 * order. This is that, and nothing else.
 */
class {{ class }} extends RuleDrivenGenerator
{
	/**
	 * The rules this generator runs: the prefix of their ids.
	 *
	 * @return  string
	 */
	public function rulePrefix(): string
	{
		return '{{ prefix }}';
	}

	/**
	 * Where those rules are written down.
	 *
	 * @return  string
	 */
	protected function ruleFile(): string
	{
		return __DIR__ . '{{ ruleFile }}';
	}
}
